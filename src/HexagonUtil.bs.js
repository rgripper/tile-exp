// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Belt_Array from "bs-platform/lib/es6/belt_Array.mjs";

function makeHexagonBuilder(size) {
  var width = 2.0 * size;
  var height = Math.sqrt(3.0) * size;
  var pointyGapOdd = size * 1.5;
  var isEven = function (value) {
    return value % 2 === 0;
  };
  var hexagon = [
    [
      width,
      height / 2.0
    ],
    [
      width * 3.0 / 4.0,
      height
    ],
    [
      width / 4.0,
      height
    ],
    [
      0.0,
      height / 2.0
    ],
    [
      width / 4.0,
      0.0
    ],
    [
      width * 3.0 / 4.0,
      0.0
    ]
  ];
  var getPosition = function (column, row) {
    return [
            column * size * 3.0 + (
              isEven(row) ? 0.0 : pointyGapOdd
            ),
            row * (Math.sqrt(3.0) * size / 2.0)
          ];
  };
  return {
          getHexagonAt: (function (column, row) {
              var match = getPosition(column, row);
              var posY = match[1];
              var posX = match[0];
              return Belt_Array.map(hexagon, (function (param) {
                            return [
                                    param[0] + posX,
                                    param[1] + posY
                                  ];
                          }));
            })
        };
}

export {
  makeHexagonBuilder ,
  
}
/* No side effect */
